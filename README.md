# gulp-starterpack setup with sass & browser-sync
## Quick start:
- Clone the repo
- cd to the cloned folder
- Run `npm install`
- Run `gulp watch`

## Any changes in .scss file with be continuously watch and compiled to css
